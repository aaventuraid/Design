#################################################################
# MINIMAL REQUIRED ENV (Coolify / Production)                   #
# Only these four values are stored in the environment.         #
# SELAIN INI semuanyaa (AI keys, limits, feature flags, dll)    #
# dikelola via database (table systemSettings / siteContent).   #
#################################################################

DATABASE_URL="postgresql://postgres:password@localhost:5432/ai_platform_dev"
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
NEXTAUTH_SECRET="your-nextauth-secret-key-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

#################################################################
# CATATAN:                                                      #
# - Admin & Gemini API Key diatur lewat Admin Panel (DB).       #
# - Limit upload, watermark, branding, dark mode, dll => DB.    #
# - Jangan tambahkan variabel baru ke file ini kecuali wajib.  #
#################################################################

