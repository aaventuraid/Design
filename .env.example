# Database Configuration
# For development (SQLite):
# DATABASE_URL="file:./dev.db"
# For production (PostgreSQL - will be provided by Coolify):
DATABASE_URL="postgresql://username:password@host:port/database"

# JWT Secret for authentication
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# NextAuth Configuration (REQUIRED for production)
NEXTAUTH_SECRET="your-nextauth-secret-key-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

#############################
# ADMIN & AI CONFIGURATION  #
#############################
# ⚠️ IMPORTANT CHANGES:
# - Admin credentials are AUTO-CREATED during database seeding
# - Default: admin@localhost / admin123 (change via Admin Panel after login)
# - Gemini API key is configured via Admin Panel (database-first approach)
# - NO environment variables needed for admin or AI configuration

# For development/testing only (creates demo premium user):
# CREATE_DEMO_USER="true"
# DEMO_USER_PASSWORD="premium123"

# Background removal provider: internal | external (external is reserved for future)
IMAGE_BG_PROVIDER=internal

# Optional: custom writable directory for settings.json when project dir is read-only
# Example: /var/data/yyc or D:\\data\\yyc (make sure the process has write permission)
DATA_DIR=
