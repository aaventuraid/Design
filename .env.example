# Admin password for saving settings via /api/admin/settings
ADMIN_PASSWORD=admin

#############################
# AI Configuration (Gemini) #
#############################
# Provide your Gemini API key here or via deployment environment settings.
# IMPORTANT: Do NOT commit real secrets. This file is an example only.
GEMINI_API_KEY=

# Background removal provider: internal | external (external is reserved for future)
IMAGE_BG_PROVIDER=internal

# Optional: custom writable directory for settings.json when project dir is read-only
# Example: /var/data/yyc or D:\\data\\yyc (make sure the process has write permission)
DATA_DIR=
