# Environment Variables untuk Coolify Deployment
# Copy variables ini ke Coolify Environment Settings

# Database Configuration (sesuaikan dengan PostgreSQL service di Coolify)
DATABASE_URL=postgresql://yuki_admin:CHANGE_THIS_PASSWORD@yuki-yaki-db:5432/yuki_yaki_production

# JWT Secret (WAJIB: Generate secure 32+ character secret)
JWT_SECRET=CHANGE_THIS_TO_SECURE_32_PLUS_CHAR_SECRET

# Admin Setup (untuk akses admin panel)
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# AI Configuration
GEMINI_API_KEY=your_gemini_api_key_here
IMAGE_BG_PROVIDER=internal

# Next.js Production Configuration
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Optional: Data directory fallback (jika filesystem read-only)
DATA_DIR=/app/data

# Optional: Rate Limiting Configuration
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=3600000
RATE_LIMIT_MAX_REQUESTS=1000

# Security Headers
ENABLE_SECURITY_HEADERS=true